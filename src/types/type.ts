export enum FetchedStatus {
  IDLE = "idle",
  ERROR = "error",
  LOADING = "loading",
  NOGAMES = "noGames",
}

export enum FilterValue {
  PRICE = 'Price',
  PUBLISHDATE = 'Publish Date'
}

export enum FilterOrder {
  DESC = 'desc',
  ASC = 'asc'
}